Types::QueryType = GraphQL::ObjectType.define do
  name "Query"
  # Add root-level fields here.
  # They will be entry points for queries on your schema.
<% if options[:relay] %>  field :node, GraphQL::Relay::Node.field
<% end %>end
